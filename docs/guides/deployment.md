# Deployment Guide

## Testnet Deployment

1. Configure settings in `settings/Testnet.toml`
2. Run `clarinet deploy --testnet`

## Mainnet Deployment

1. Configure settings in `settings/Mainnet.toml`
2. Run `clarinet deploy --mainnet`

## Frontend Deployment

The frontend can be deployed to Vercel or any static hosting provider.
