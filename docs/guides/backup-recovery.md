# Backup and Recovery

## Contract Data

All contract data is stored on the Stacks blockchain and is inherently backed up by the network.

## Frontend State

The frontend is stateless and can be redeployed from the repository at any time.

## Development Environment

Key files to preserve:
- `.env` - Environment variables
- `Clarinet.toml` - Project configuration
- `settings/` - Network configurations
