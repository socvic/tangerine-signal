# Clarinet Configuration

Clarinet.toml is the project configuration file for the Clarity development environment.

## Key Settings

- `name`: Project name
- `requirements`: List of contract requirements
- `contracts`: Contract paths and configurations

## Devnet Configuration

Settings in `settings/Devnet.toml` control the local development network:
- Network duration
- Initial balances
- Contract deployments
