# Architecture Overview

## Smart Contract Layer

The core smart contract `micro-polls.clar` handles poll creation, voting, and result tracking on the Stacks blockchain.

## Frontend Layer

Built with React + Vite, the frontend provides a user interface for interacting with the micro-polls contract.

## Testing

Tests are written using Vitest and @hirosystems/clarinet-sdk to ensure contract correctness.
