# Styling Guide

## Approach

The project uses CSS with component-scoped styles.

## Conventions

- Use CSS custom properties for theming
- Follow BEM naming convention for classes
- Keep styles close to components
- Use relative units (rem, em) over pixels
