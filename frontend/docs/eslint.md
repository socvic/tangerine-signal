# ESLint Configuration

The frontend uses ESLint with the flat config format.

## Configuration File

`eslint.config.js` contains the ESLint rules.

## Key Rules

- React hooks rules
- TypeScript strict checks
- Import ordering
- No unused variables

## Running Lint

```bash
npm run lint
```
